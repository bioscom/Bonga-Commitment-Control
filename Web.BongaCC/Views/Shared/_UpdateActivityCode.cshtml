@model Web.BongaCC.ViewModels.ActivityCodeViewModel

<div class="row">
    <div class="col-md-12">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <div class="form-group row">
            <div class="col-md-4">
                <label asp-for="ActivityCodeWorkStreamID" class="control-label"></label>
                <select asp-for="ActivityCodeWorkStreamID" asp-items="ViewBag.WorkStream" class="form-control">
                    <option disabled selected>--- SELECT ---</option>
                </select>
            </div>
            <div class="col-md-8">
                <label asp-for="ActivityCodeDesc" class="control-label"></label>
                <input asp-for="ActivityCodeDesc" class="form-control" />
                <span asp-validation-for="ActivityCodeDesc" class="text-danger"></span>
            </div>
        </div>

        <div class="form-group row">
            <div class="col-md-6">
                <label asp-for="ActivityOwnerID" class="control-label"></label>
                <select asp-for="ActivityOwnerID" asp-items="ViewBag.ActivityOwner" class="form-control">
                    <option disabled selected>--- SELECT ---</option>
                </select>
            </div>
            <div class="col-md-6">
                <label class="control-label">Alternate</label>
                <select asp-for="ActivityOwnerID" asp-items="ViewBag.ActivityOwner" class="form-control">
                    <option disabled selected>--- SELECT ---</option>
                </select>
            </div>
        </div>

        <div class="form-group row">
            <div class="col-md-6">
                <label asp-for="LineManagerID" class="control-label"></label>
                <select asp-for="LineManagerID" asp-items="ViewBag.LineManager" class="form-control">
                    <option disabled selected>--- SELECT ---</option>
                </select>
            </div>
            <div class="col-md-6">
                <label class="control-label">Alternate</label>
                <select asp-for="LineManagerID" asp-items="ViewBag.LineManager" class="form-control">
                    <option disabled selected>--- SELECT ---</option>
                </select>
            </div>
        </div>

        <div class="form-group row">
            <div class="col-md-6">
                <label asp-for="AccountableManagerID" class="control-label"></label>
                <select asp-for="AccountableManagerID" asp-items="ViewBag.AccountableManager" class="form-control">
                    <option disabled selected>--- SELECT ---</option>
                </select>
            </div>
            <div class="col-md-6">
                <label class="control-label">Alternate</label>
                <select asp-for="AccountableManagerID" asp-items="ViewBag.AccountableManager" class="form-control">
                    <option disabled selected>--- SELECT ---</option>
                </select>
            </div>
        </div>

        <input type="hidden" asp-for="ID" />
    </div>
</div>